extends base.pug

block head
    h1 #{title}

block content
    form(method="POST")
        input(placeholder="이메일", required, type="email", name="email")
        input(placeholder="이름", required, type="text", name="name")
        input(placeholder="비밀번호", required, type="password", name="password")
        input(placeholder="비밀번호 확인", required, type="password", name="password2")
        input(type="submit", value="create")